# hello-world
My first repository to create a program that prints "Hello, World!" to the standard output.

As a test codebase, I will periodically modify this Python Script in order to learn about
"git", such as setting-up SSH Keys, "cloning" repositories, and using the "git" CLI commands
from my local machine, in order to: "add", "commit", and "push" files from my local (remote)
machine to my GitHub Repository.
